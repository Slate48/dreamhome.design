{
  "name": "User",
  "type": "object",
  "properties": {
    "role": {
      "type": "string",
      "enum": [
        "client",
        "manager",
        "admin",
        "super_admin"
      ],
      "default": "client",
      "description": "User role: client (portal access only), manager (admin portal read/manage), admin (full admin access), super_admin (full access + user management)"
    }
  },
  "required": []
}